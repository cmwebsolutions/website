<section class="section" id="pricing">
    <div class="container">
        <div class="pricing-grid">
            <div class="pricing-card">
                <span class="eyebrow">Simple pricing</span>

                <h2>Professional website. Predictable cost.</h2>

                <div class="price">
                    <strong>&pound;149</strong>
                    <span>/month</span>
                </div>

                <p>
                    No upfront website build cost. 12-month minimum term.
                </p>
            </div>

            <div class="pricing-details">
                <h3>Included in the &pound;149/month plan</h3>

                <ul class="check-list">
                    <li>
                        <span class="check">&#10003;</span>
                        <span>Professional brochure-style website design and build</span>
                    </li>

                    <li>
                        <span class="check">&#10003;</span>
                        <span>Mobile-friendly responsive layout</span>
                    </li>

                    <li>
                        <span class="check">&#10003;</span>
                        <span>Website hosting included</span>
                    </li>

                    <li>
                        <span class="check">&#10003;</span>
                        <span>Maintenance, updates and basic support</span>
                    </li>

                    <li>
                        <span class="check">&#10003;</span>
                        <span>Basic on-page SEO setup for core pages</span>
                    </li>

                    <li>
                        <span class="check">&#10003;</span>
                        <span>12-month minimum term</span>
                    </li>
                </ul>

                <div style="margin-top: 30px;">
                    <a class="btn btn-primary" href="#consultation">Book a free consultation</a>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .pricing-grid {
        display: grid;
        grid-template-columns: 0.95fr 1.05fr;
        gap: 18px;
    }

    .pricing-card {
        position: relative;
        overflow: hidden;
        padding: clamp(36px, 5vw, 58px);
        color: var(--white);
        background:
            radial-gradient(circle at 88% 0%, rgba(15, 210, 187, 0.38), transparent 18rem),
            linear-gradient(135deg, var(--primary), #250071);
        border-radius: var(--radius-xl);
    }

    .pricing-card :global(h2) {
        color: var(--white);
    }

    .pricing-card :global(.eyebrow) {
        color: var(--white);
        background: rgba(252, 252, 252, 0.12);
        border-color: rgba(252, 252, 252, 0.18);
    }

    .price {
        display: flex;
        align-items: flex-end;
        gap: 8px;
        margin: 30px 0 12px;
    }

    .price strong {
        font-size: clamp(4.4rem, 8vw, 7.2rem);
        font-weight: 800;
        line-height: 0.82;
    }

    .price span {
        padding-bottom: 10px;
        color: rgba(252, 252, 252, 0.70);
        font-weight: 800;
    }

    .pricing-card p {
        color: rgba(252, 252, 252, 0.72);
    }

    .pricing-details {
        padding: clamp(32px, 5vw, 50px);
        background: var(--surface);
        border: 1px solid var(--border);
        border-radius: var(--radius-xl);
    }

    @media (max-width: 980px) {
        .pricing-grid {
            grid-template-columns: 1fr;
        }
    }
</style>