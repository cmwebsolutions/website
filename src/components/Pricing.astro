<section class="section" id="pricing">
    <div class="container">
        <div class="pricing-grid">
            <div class="pricing-card">
                <span class="eyebrow">Simple pricing</span>

                <h2>Professional website. Predictable cost.</h2>

                <div class="price">
                    <strong>&pound;249</strong>
                    <span>/month</span>
                </div>

                <p>
                    &pound;499 one-off setup fee. 12-month minimum agreement.
                </p>
            </div>

            <div class="pricing-details">
                <h3>What's included in your managed plan</h3>

                <ul class="check-list">
                    <li>
                        <span class="check">&#10003;</span>
                        <span>Professionally designed and developed website (up to 5 pages)</span>
                    </li>

                    <li>
                        <span class="check">&#10003;</span>
                        <span>Premium hosting with SSL certificate, backups, monitoring and security</span>
                    </li>

                    <li>
                        <span class="check">&#10003;</span>
                        <span>Fast, lightweight, performance-focused build</span>
                    </li>

                    <li>
                        <span class="check">&#10003;</span>
                        <span>Ongoing minor text and image content updates</span>
                    </li>

                    <li>
                        <span class="check">&#10003;</span>
                        <span>Ongoing technical support and maintenance</span>
                    </li>

                    <li>
                        <span class="check">&#10003;</span>
                        <span>12-month minimum agreement</span>
                    </li>
                </ul>

                <div style="margin-top: 30px;">
                    <a class="btn btn-primary" href="#consultation">Book a free consultation</a>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .pricing-grid {
        display: grid;
        grid-template-columns: 0.95fr 1.05fr;
        gap: 18px;
    }

    .pricing-card {
        position: relative;
        overflow: hidden;
        padding: clamp(36px, 5vw, 58px);
        color: var(--white);
        background:
            radial-gradient(circle at 88% 0%, rgba(15, 210, 187, 0.38), transparent 18rem),
            linear-gradient(135deg, var(--primary), #250071);
        border-radius: var(--radius-xl);
    }

    .pricing-card :global(h2) {
        color: var(--white);
    }

    .pricing-card :global(.eyebrow) {
        color: var(--white);
        background: rgba(252, 252, 252, 0.12);
        border-color: rgba(252, 252, 252, 0.18);
    }

    .price {
        display: flex;
        align-items: flex-end;
        gap: 8px;
        margin: 30px 0 12px;
    }

    .price strong {
        font-size: clamp(4.4rem, 8vw, 7.2rem);
        font-weight: 800;
        line-height: 0.82;
    }

    .price span {
        padding-bottom: 10px;
        color: rgba(252, 252, 252, 0.70);
        font-weight: 800;
    }

    .pricing-card p {
        color: rgba(252, 252, 252, 0.72);
    }

    .pricing-details {
        padding: clamp(32px, 5vw, 50px);
        background: var(--surface);
        border: 1px solid var(--border);
        border-radius: var(--radius-xl);
    }

    @media (max-width: 980px) {
        .pricing-grid {
            grid-template-columns: 1fr;
        }
    }
</style>