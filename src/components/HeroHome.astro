<section class="hero-home">
    <div class="hero-bg"></div>
    <div class="container">
        <div class="hero-grid">
            <div class="hero-content">
                <span class="eyebrow">CM Web Solutions</span>
                <h1>Design. Build. Grow.<br />Your website done right.</h1>
                <p class="lead">
                    We build high-performing websites for UK businesses, from single landing pages to full multi-page sites. No fluff, just purposeful design that drives results.
                </p>
                <div class="hero-ctas">
                    <a class="btn btn-accent" href="/work">View our work</a>
                    <a class="btn btn-ghost" href="/contact">Book a consultation</a>
                </div>
                <div class="hero-pills">
                    <span class="pill">Landing pages from £1,750</span>
                    <span class="pill">5-page sites from £2,750</span>
                    <span class="pill">10-page sites from £4,250</span>
                </div>
            </div>
            <div class="hero-visual">
                <div class="hero-card">
                    <div class="hero-card-dots">
                        <span></span><span></span><span></span>
                    </div>
                    <div class="hero-card-content">
                        <div class="hero-card-header">
                            <span class="hero-card-site">cmwebsolutions.com</span>
                            <span class="hero-card-status">Live</span>
                        </div>
                        <div class="hero-card-graphic">
                            <div class="graphic-bar graphic-bar--l"></div>
                            <div class="graphic-bar graphic-bar--m"></div>
                            <div class="graphic-bar graphic-bar--s"></div>
                        </div>
                        <div class="hero-card-stats">
                            <div class="hero-stat">
                                <span class="hero-stat-value">3x</span>
                                <span class="hero-stat-label">faster load</span>
                            </div>
                            <div class="hero-stat">
                                <span class="hero-stat-value">100%</span>
                                <span class="hero-stat-label">responsive</span>
                            </div>
                            <div class="hero-stat">
                                <span class="hero-stat-value">99.9%</span>
                                <span class="hero-stat-label">uptime</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .hero-home {
        position: relative;
        padding: 144px 0 112px;
        color: var(--white);
        overflow: hidden;
    }

    .hero-bg {
        position: absolute;
        inset: 0;
        background: radial-gradient(ellipse 80% 60% at 50% 20%, rgba(65, 0, 245, 0.35) 0%, transparent 100%),
                    radial-gradient(ellipse 60% 50% at 80% 80%, rgba(15, 210, 187, 0.15) 0%, transparent 100%),
                    var(--black);
        z-index: 0;
    }

    .hero-bg::after {
        content: "";
        position: absolute;
        inset: 0;
        background-image:
            linear-gradient(rgba(252, 252, 252, 0.03) 1px, transparent 1px),
            linear-gradient(90deg, rgba(252, 252, 252, 0.03) 1px, transparent 1px);
        background-size: 60px 60px;
    }

    .hero-grid {
        position: relative;
        z-index: 1;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 60px;
        align-items: center;
    }

    .hero-content :global(.eyebrow) {
        color: var(--accent);
        background: rgba(15, 210, 187, 0.1);
        border-color: rgba(15, 210, 187, 0.2);
    }

    .hero-content :global(.eyebrow)::before {
        background: var(--accent);
        box-shadow: 0 0 0 5px rgba(15, 210, 187, 0.18);
    }

    .hero-content h1 {
        max-width: 100%;
        color: var(--white);
    }

    .hero-content :global(.lead) {
        max-width: 520px;
        color: var(--dark-muted);
    }

    .hero-ctas {
        display: flex;
        flex-wrap: wrap;
        gap: 16px;
        margin-top: 36px;
    }

    .hero-pills {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin-top: 42px;
    }

    .hero-pills :global(.pill) {
        font-size: 0.82rem;
    }

    /* Visual card */
    .hero-card {
        background: rgba(252, 252, 252, 0.06);
        border: 1px solid rgba(252, 252, 252, 0.1);
        border-radius: var(--radius-md);
        backdrop-filter: blur(12px);
        overflow: hidden;
    }

    .hero-card-dots {
        display: flex;
        gap: 8px;
        padding: 14px 18px;
        background: rgba(252, 252, 252, 0.04);
        border-bottom: 1px solid rgba(252, 252, 252, 0.06);
    }

    .hero-card-dots span {
        width: 10px;
        height: 10px;
        border-radius: 999px;
        background: rgba(252, 252, 252, 0.15);
    }

    .hero-card-dots span:first-child { background: #FF5F57; }
    .hero-card-dots span:nth-child(2) { background: #FEBC2E; }
    .hero-card-dots span:nth-child(3) { background: #28C840; }

    .hero-card-content {
        padding: 28px;
    }

    .hero-card-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 24px;
    }

    .hero-card-site {
        font-size: 0.92rem;
        font-weight: 700;
        color: var(--white);
    }

    .hero-card-status {
        font-size: 0.78rem;
        font-weight: 700;
        padding: 4px 10px;
        border-radius: 999px;
        color: var(--accent);
        background: rgba(15, 210, 187, 0.12);
        border: 1px solid rgba(15, 210, 187, 0.2);
    }

    .hero-card-graphic {
        display: flex;
        flex-direction: column;
        gap: 10px;
        margin-bottom: 28px;
    }

    .graphic-bar {
        height: 12px;
        border-radius: 999px;
        background: rgba(252, 252, 252, 0.08);
        position: relative;
        overflow: hidden;
    }

    .graphic-bar::after {
        content: "";
        position: absolute;
        inset: 0;
        border-radius: 999px;
        background: linear-gradient(90deg, var(--primary), var(--accent));
        opacity: 0.7;
    }

    .graphic-bar--l::after { width: 85%; }
    .graphic-bar--m::after { width: 65%; }
    .graphic-bar--s::after { width: 45%; }

    .hero-card-stats {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 12px;
    }

    .hero-stat {
        text-align: center;
        padding: 14px 8px;
        background: rgba(252, 252, 252, 0.04);
        border-radius: var(--radius-sm);
        border: 1px solid rgba(252, 252, 252, 0.06);
    }

    .hero-stat-value {
        display: block;
        font-size: 1.4rem;
        font-weight: 900;
        color: var(--accent);
        line-height: 1;
    }

    .hero-stat-label {
        display: block;
        margin-top: 4px;
        font-size: 0.76rem;
        color: var(--dark-muted);
    }

    @media (max-width: 980px) {
        .hero-home {
            padding: 124px 0 80px;
        }

        .hero-grid {
            grid-template-columns: 1fr;
            gap: 48px;
        }

        .hero-visual {
            max-width: 480px;
        }
    }

    @media (max-width: 640px) {
        .hero-home {
            padding: 110px 0 64px;
        }

        .hero-ctas :global(.btn) {
            width: 100%;
        }

        .hero-pills {
            flex-direction: column;
        }

        .hero-card-stats {
            grid-template-columns: 1fr;
        }
    }
</style>
