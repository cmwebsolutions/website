<section class="section section-soft" id="faq">
    <div class="container">
        <div class="faq-grid">
            <div class="faq-copy">
                <span class="eyebrow">FAQ</span>

                <h2>Clear answers before you commit.</h2>

                <p class="lead">
                    If you are unsure whether the subscription model is right for your business, book a free consultation and we'll talk through it.
                </p>
            </div>

            <div class="faq-list">
                <details>
                    <summary>Is there a minimum term?</summary>
                    <p>
                        Yes. The website subscription has a 12-month minimum term. This allows the design, build, launch and support to be spread across the subscription.
                    </p>
                </details>

                <details>
                    <summary>Who owns the website?</summary>
                    <p>
                        Ownership terms are confirmed in your agreement. A common approach is that the website transfers after the minimum term is complete and payments are up to date.
                    </p>
                </details>

                <details>
                    <summary>Can I upgrade later?</summary>
                    <p>
                        Yes. If you need extra pages, ecommerce, booking systems, landing pages or ongoing marketing support, we can move you onto a more suitable plan.
                    </p>
                </details>

                <details>
                    <summary>What counts as support?</summary>
                    <p>
                        Support usually includes hosting-related help, basic maintenance, updates and reasonable minor changes. Larger new features or new pages may be quoted separately.
                    </p>
                </details>

                <details>
                    <summary>Is ongoing SEO included?</summary>
                    <p>
                        Basic SEO setup is included. Ongoing SEO campaigns, content creation, technical SEO and link building are separate services.
                    </p>
                </details>
            </div>
        </div>
    </div>
</section>

<style>
    .faq-grid {
        display: grid;
        grid-template-columns: 0.78fr 1.22fr;
        gap: 42px;
        align-items: start;
    }

    .faq-copy {
        position: sticky;
        top: 110px;
    }

    .faq-list {
        display: grid;
        gap: 12px;
    }

    details {
        background: var(--surface);
        border: 1px solid var(--border);
        border-radius: 18px;
        overflow: hidden;
    }

    summary {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 18px;
        padding: 20px 22px;
        color: var(--black);
        font-weight: 800;
        cursor: pointer;
        list-style: none;
    }

    summary::-webkit-details-marker {
        display: none;
    }

    summary::after {
        content: "+";
        width: 28px;
        height: 28px;
        display: grid;
        place-items: center;
        flex: 0 0 auto;
        color: var(--white);
        background: var(--primary);
        border-radius: 999px;
        font-weight: 800;
        transition: transform 180ms ease;
    }

    details[open] summary::after {
        transform: rotate(45deg);
    }

    details p {
        padding: 0 22px 22px;
        font-size: 0.94rem;
    }

    @media (max-width: 980px) {
        .faq-grid {
            grid-template-columns: 1fr;
        }

        .faq-copy {
            position: static;
        }
    }
</style>