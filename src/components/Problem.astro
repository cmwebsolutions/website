<section class="section section-soft">
    <div class="container">
        <div class="problem-grid">
            <div class="problem-intro">
                <span class="eyebrow">The problem</span>

                <h2>Traditional website projects are awkward for small businesses.</h2>

                <p>
                    They're expensive upfront, slow to start, and often leave you managing hosting, updates and fixes yourself after launch.
                </p>
            </div>

            <div class="problem-cards">
                <article class="card">
                    <h3>Hidden costs add up</h3>
                    <p>
                        Scattered hosting, security, updates and support bills turn a simple website into a recurring headache.
                    </p>
                </article>

                <article class="card">
                    <h3>DIY builders still take time</h3>
                    <p>
                        Drag-and-drop tools are only easy until you need the site to look serious and perform well for the business.
                    </p>
                </article>

                <article class="card">
                    <h3>Support is usually separate</h3>
                    <p>
                        Hosting, security, updates, changes and fixes quickly become another thing on your plate.
                    </p>
                </article>
            </div>
        </div>
    </div>
</section>

<style>
    .problem-grid {
        display: grid;
        grid-template-columns: 0.86fr 1.14fr;
        gap: 18px;
    }

    .problem-intro {
        padding: clamp(32px, 5vw, 54px);
        color: var(--white);
        background:
            radial-gradient(circle at 100% 0%, rgba(15, 210, 187, 0.25), transparent 18rem),
            var(--black);
        border-radius: var(--radius-xl);
    }

    .problem-intro :global(.eyebrow) {
        color: var(--accent);
        background: rgba(15, 210, 187, 0.12);
        border-color: rgba(15, 210, 187, 0.24);
    }

    .problem-intro :global(h2) {
        color: var(--white);
        font-size: clamp(2.25rem, 4.8vw, 2.4rem);
    }

    .problem-intro p {
        margin-top: 18px;
        color: var(--dark-muted);
    }

    .problem-cards {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 14px;
    }

    .problem-cards :global(.card):first-child {
        grid-column: span 2;
        color: var(--white);
        background: var(--primary);
        border-color: rgba(65, 0, 245, 0.14);
    }

    .problem-cards :global(.card):first-child :global(p) {
        color: rgba(252, 252, 252, 0.72);
    }

    @media (max-width: 980px) {
        .problem-grid {
            grid-template-columns: 1fr;
        }
    }

    @media (max-width: 640px) {
        .problem-cards {
            grid-template-columns: 1fr;
        }

        .problem-cards :global(.card):first-child {
            grid-column: span 1;
        }
    }
</style>
